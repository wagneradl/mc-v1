# ── Authentication ────────────────────────────────────
# Bearer token for HTTPS access (generate with: openssl rand -hex 32)
MCP_BEARER_TOKEN=

# ── Domain ────────────────────────────────────────────
# Public domain for Caddy TLS auto-provisioning
DOMAIN=mcp.yourdomain.com

# ── MCP Server API Keys ──────────────────────────────
# GitHub: https://github.com/settings/tokens
GITHUB_TOKEN=

# Brave Search: https://brave.com/search/api/
BRAVE_API_KEY=

# Todoist: https://todoist.com/prefs/integrations
TODOIST_API_TOKEN=

# Firecrawl: https://firecrawl.dev/
FIRECRAWL_API_KEY=

# ── OAuth Authorization Server (Phase 4 — ChatGPT integration) ──
# Password for OAuth login screen (generate with: openssl rand -hex 16)
OAUTH_AUTHORIZE_PASSWORD=
# MCP_BEARER_TOKEN already exists above — reused as OAuth access_token
